name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "show path"
      - run: pwd
      - run: echo "show content"
      - run: ls -la
